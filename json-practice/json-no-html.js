// Test making a webpage in JS w/o an HTML template

// SECTION code

// SECTION template build
  // template var
let tmplt = `<h1>Weather details and JS w/o HTML template</h1>`;

document.write(tmplt);